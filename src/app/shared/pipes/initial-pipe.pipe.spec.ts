import { InitialPipePipe } from './initial-pipe.pipe';

describe('InitialPipePipe', () => {
  it('create an instance', () => {
    const pipe = new InitialPipePipe();
    expect(pipe).toBeTruthy();
  });
});
